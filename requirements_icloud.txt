# 纯协议 ChatGPT 自动注册 + iCloud + SUB2API
# 基于 FlowPilot + chatgpt-auto-register 逆向

# 核心
curl_cffi>=0.6            # Chrome TLS 指纹伪装 → 绕过 Cloudflare
requests>=2.25            # HTTP 请求

# iCloud Chrome cookie 解密 (Windows)
pycryptodome>=3.15
pywin32>=305; platform_system == "Windows"

# 浏览器自动化 (旧版 chatgpt_signup.py, 已弃用)
# playwright>=1.40
